import { type ReactElement } from "react";

export default function Projects(): ReactElement {
   return (
      <section className='pt-24 pb-24 pl-40 pr-40 bg-amber-200'>
         projects
      </section>
   );
}
