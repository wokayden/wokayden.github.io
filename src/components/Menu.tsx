import type { ReactElement } from 'react';

export default function Menu(): ReactElement {


    return (
        <header className="sticky bg-black top-full">
            header hi!
        </header>
    )
}